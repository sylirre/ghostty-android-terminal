<?xml version="1.0" encoding="utf-8"?>
<!-- Terminal scrollbar thumb: a slim rounded accent bar (semi-translucent so
     it reads on any theme background) instead of the platform-grey default. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#8C7C6CF6" />
    <corners android:radius="@dimen/radius_pill" />
</shape>
