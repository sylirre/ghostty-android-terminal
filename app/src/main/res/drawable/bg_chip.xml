<?xml version="1.0" encoding="utf-8"?>
<!-- Tappable pill chip with an accent-tinted ripple. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/accent_translucent">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/on_accent" />
            <corners android:radius="@dimen/radius_md" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/surface_2" />
            <corners android:radius="@dimen/radius_md" />
            <stroke android:width="@dimen/stroke_hairline" android:color="@color/border" />
        </shape>
    </item>
</ripple>
